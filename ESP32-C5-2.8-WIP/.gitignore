# PlatformIO build output + this board's isolated core (toolchains, frameworks)
.pio/
.pio-core/

# Arduino preprocessor output (regenerated every build)
WontHound-CYD.ino.cpp

# Cloned vendor reference repo + third-party firmwares (not ours to redistribute)
rockbase-ref/
reference-firmwares/

# Bench logs / scratch
logs/
.agents/

# Editor
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch

# Keep the ready-to-flash binaries (flash/) and source in the repo.
!flash/
